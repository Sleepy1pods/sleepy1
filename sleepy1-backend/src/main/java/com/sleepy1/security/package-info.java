/**
 * Authentication and authorisation (JWT issuance/validation, password hashing, role
 * guards). Empty placeholder — no authentication is implemented yet. The frontend's
 * current mock auth (src/services/authService.ts) is not secure and must be replaced
 * by a real implementation in this package before production use.
 */
package com.sleepy1.security;
