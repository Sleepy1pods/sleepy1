/**
 * REST controllers exposing the Sleepy1 API (locations, bookings, auth, payments, etc.).
 * Empty placeholder — see docs/api-contract.md for the proposed endpoint contract that
 * these controllers will eventually implement.
 */
package com.sleepy1.controller;
